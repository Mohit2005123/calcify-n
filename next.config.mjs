/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'images.pexels.com', 'assets.aceternity.com'],
    },
};

export default nextConfig;
